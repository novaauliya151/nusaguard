"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import styles from "../login/auth.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Forgot(){const[email,setEmail]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch(`${API}/api/user/password-reset/request`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setMessage((await r.json()).message)}finally{setBusy(false)}}return <main className={styles.shell}><section className={styles.formSide}><div className={styles.formWrap}><p className={styles.kicker}>PEMULIHAN AKUN</p><h1>Lupa kata sandi?</h1><p>Masukkan email. Pesan yang sama ditampilkan, baik email terdaftar maupun tidak.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button disabled={busy}>{busy?"Memproses…":"Kirim petunjuk reset"}</button></form>{message&&<p>{message}</p>}<Link className={styles.back} href="/masuk">Kembali ke Masuk</Link></div></section></main>}
