"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import styles from "../content.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";const categories=["Phishing/Link Berbahaya","Social Engineering","Penipuan Investasi","Penipuan Rekrutmen","Penipuan Romansa"];
export default function Report(){const[text,setText]=useState(""),[category,setCategory]=useState(categories[0]),[consent,setConsent]=useState(false),[status,setStatus]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");const r=await fetch(`${API}/api/report`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text,category_suggested:category,consent})});if(r.ok){setStatus("Laporan diterima. Admin akan meninjau dan menganonimkannya sebelum dapat masuk dataset publik.");setText("");setConsent(false)}else setError((await r.json()).detail??"Laporan gagal dikirim.")}return <main className={styles.page}><header className={styles.header}><Link className={styles.brand} href="/"><span>NG</span>NusaGuard</Link><nav><Link href="/education">Edukasi</Link><Link href="/dataset">Dataset publik</Link></nav></header><div className={styles.formPage}><section className={styles.formIntro}><p className={styles.kicker}>LAPOR PENIPUAN</p><h1>Laporanmu membantu melindungi yang lain.</h1><p>Pesan yang kamu kirim masuk antrean moderasi. Hanya laporan yang disetujui, ditinjau, dan dianonimkan yang dapat menjadi dataset publik.</p><div className={styles.privacy}><h3>Jangan sertakan</h3><p>Nama lengkap, alamat, nomor telepon, email, rekening, OTP, PIN, atau identitas korban. NusaGuard juga menjalankan penyamaran otomatis sebelum publikasi.</p></div></section><section className={styles.formWrap}><form onSubmit={submit}><p className={styles.kicker}>FORMULIR CONSENT</p><h2>Detail pesan penipuan</h2><label>Kategori<select value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(x=><option key={x}>{x}</option>)}</select></label><label>Isi pesan<textarea required maxLength={5000} value={text} onChange={e=>setText(e.target.value)} placeholder="Tempel contoh pesan tanpa identitas pribadi…"/></label><label className={styles.consent}><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/><span>Saya menyetujui laporan ini disimpan, ditinjau, dianonimkan, dan bila layak dipublikasikan sebagai dataset NusaGuard.</span></label><button disabled={!consent||!text.trim()}>Kirim laporan →</button>{status&&<div className={styles.success}>{status}</div>}{error&&<p className={styles.error}>{error}</p>}</form></section></div></main>}

