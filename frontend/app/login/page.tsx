"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import styles from "./auth.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch(`${API}/api/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const body=await r.json();if(!r.ok)throw new Error(body.detail);localStorage.setItem("nusaguard_token",body.access_token);router.push("/dashboard")}catch(x){setError(x instanceof Error?x.message:"Login gagal.")}finally{setLoading(false)}}return <main className={styles.shell}><section className={styles.formSide}><Link className={styles.brand} href="/"><span>NG</span><b>NusaGuard</b></Link><div className={styles.formWrap}><p className={styles.kicker}>AKUN PENGGUNA</p><h1>Selamat datang kembali.</h1><p>Masuk untuk mengakses pusat keamanan dan seluruh fitur akunmu.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="nama@email.com"/></label><label>Kata sandi<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<div className={styles.error}>{error}</div>}<button disabled={loading}>{loading?"Memeriksa…":"Masuk →"}</button></form><p className={styles.switch}>Belum punya akun? <Link href="/register">Daftar sekarang</Link></p><Link className={styles.back} href="/">← Analisis tanpa login</Link></div></section><Visual/></main>}
function Visual(){return <section className={styles.visual}><span>PERLINDUNGAN YANG KAMU KENDALIKAN</span><h2>Satu akun.<br/>Semua fitur aman.</h2><div><p>01<small>Analisis pesan tetap ephemeral</small></p><p>02<small>Akses edukasi dan rekomendasi</small></p><p>03<small>Kirim laporan dengan persetujuan</small></p></div></section>}

