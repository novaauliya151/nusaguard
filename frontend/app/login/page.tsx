"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import styles from "./auth.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
function Eye({open}:{open:boolean}){return <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M2.5 12s3.5-6 9.5-6 9.5 6 9.5 6-3.5 6-9.5 6-9.5-6-9.5-6Z"/><circle cx="12" cy="12" r="2.7"/>{open&&<path d="m4 4 16 16"/>}</svg>}
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[remember,setRemember]=useState(false),[error,setError]=useState(""),[loading,setLoading]=useState(false),router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const response=await fetch(`${API}/api/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,remember_me:remember})}),body=await response.json();if(!response.ok)throw new Error(body.detail);localStorage.setItem("nusaguard_token",body.access_token);const adminAccess=body.user.permissions.some((permission:string)=>["dashboard.view","users.view","reports.view","datasets.view","education.view","models.view","settings.manage"].includes(permission));router.push(adminAccess?"/admin":"/dashboard")}catch(x){setError(x instanceof Error?x.message:"Login gagal.")}finally{setLoading(false)}}return <main className={styles.shell}><section className={styles.formSide}><Link className={styles.brand} href="/"><span>NG</span><b>NusaGuard</b></Link><div className={styles.formWrap}><p className={styles.kicker}>MASUK NUSAGUARD</p><h1>Selamat datang kembali.</h1><p>Masuk untuk membuka dashboard dan riwayat pribadimu.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="nama@email.com" autoComplete="email"/></label><label>Kata sandi<div className={styles.passwordField}><input type={show?"text":"password"} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" autoComplete="current-password"/><button type="button" onClick={()=>setShow(value=>!value)} aria-label={show?"Sembunyikan kata sandi":"Tampilkan kata sandi"}><Eye open={show}/></button></div></label><div className={styles.formOptions}><label><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Ingat saya</label><Link href="/lupa-password">Lupa kata sandi?</Link></div>{error&&<div className={styles.error}>{error}</div>}<button disabled={loading}>{loading?"Memeriksa…":"Masuk"}</button></form><p className={styles.switch}>Belum punya akun? <Link href="/daftar">Buat akun</Link></p><Link className={styles.back} href="/">Analisis tanpa login</Link></div></section><Visual/></main>}
function Visual(){return <section className={styles.visual}><span>PERLINDUNGAN YANG KAMU KENDALIKAN</span><h2>Satu akun.<br/>Semua fitur aman.</h2><div><p>01<small>Riwayat hanya disimpan dengan pilihanmu</small></p><p>02<small>Teks sensitif dianonimkan lebih dahulu</small></p><p>03<small>Analisis tetap tersedia tanpa login</small></p></div></section>}
