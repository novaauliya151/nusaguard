"use client";
import Link from "next/link";import {useEffect,useState} from "react";import styles from "../content.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";type Item={id:string;title:string;category:string;description:string;warning_signs:string[];prevention:string[]};
export default function Education(){const[items,setItems]=useState<Item[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{let active=true;fetch(`${API}/api/education`).then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{if(active)setItems(x)}).catch(()=>{if(active)setError("Edukasi belum dapat dimuat. Pastikan backend NusaGuard aktif, lalu muat ulang halaman.")}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[]);return <main className={styles.page}><Header/><section className={styles.hero}><p className={styles.kicker}>EDUKASI DINAMIS DARI NUSAGUARD</p><h1>Kenali modus.<br/>Putus rantai penipuan.</h1><p>Konten awal tersedia sejak pemasangan pertama dan dapat diperbarui admin.</p></section><section className={styles.grid}>{loading?<div className={styles.empty}>Memuat edukasi…</div>:error?<div className={styles.empty}><h2>Edukasi tidak tersedia</h2><p className={styles.error}>{error}</p></div>:items.length?items.map(item=><article className={styles.card} key={item.id}><span>{item.category}</span><h2>{item.title}</h2><p>{item.description}</p><h3>Tanda peringatan</h3><ul>{item.warning_signs.map(x=><li key={x}>{x}</li>)}</ul><h3>Pencegahan</h3><ul>{item.prevention.map(x=><li key={x}>{x}</li>)}</ul></article>):<div className={styles.empty}><h2>Konten sedang disiapkan</h2></div>}</section><Footer/></main>}
function Header(){return <header className={styles.header}><Link className={styles.brand} href="/"><span>NG</span>NusaGuard</Link><nav><Link href="/statistics">Statistik</Link><Link href="/report">Lapor penipuan</Link><Link href="/dataset">Dataset publik</Link><Link href="/login">Masuk</Link></nav></header>}function Footer(){return <footer className={styles.footer}><span>NusaGuard · Edukasi</span><span>Konten dikelola admin</span></footer>}
