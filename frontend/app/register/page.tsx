"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import styles from "../login/auth.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Register(){const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();if(password!==confirm){setError("Konfirmasi kata sandi tidak sama.");return}setLoading(true);setError("");try{const r=await fetch(`${API}/api/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,email,password})});const body=await r.json();if(!r.ok)throw new Error(body.detail);localStorage.setItem("nusaguard_token",body.access_token);router.push("/dashboard")}catch(x){setError(x instanceof Error?x.message:"Registrasi gagal.")}finally{setLoading(false)}}return <main className={styles.shell}><section className={styles.formSide}><Link className={styles.brand} href="/"><span>NG</span><b>NusaGuard</b></Link><div className={styles.formWrap}><p className={styles.kicker}>PENDAFTARAN AKUN</p><h1>Mulai lebih aman.</h1><p>Akun baru otomatis memperoleh role User dan hak akses dasar.</p><form onSubmit={submit}><label>Nama lengkap<input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Kata sandi<input required type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimal 8 karakter"/></label><label>Konfirmasi kata sandi<input required type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className={styles.error}>{error}</div>}<button disabled={loading}>{loading?"Membuat akun…":"Daftar & masuk →"}</button></form><p className={styles.switch}>Sudah punya akun? <Link href="/login">Masuk</Link></p></div></section><section className={styles.visual}><span>DEFAULT ROLE · USER</span><h2>Privasi sejak<br/>akun pertama.</h2><div><p>✓<small>Password di-hash PBKDF2, tidak disimpan mentah</small></p><p>✓<small>Sesi dapat dinonaktifkan oleh admin</small></p><p>✓<small>Pesan analisis tetap tidak masuk histori</small></p></div></section></main>}

