/* eslint-disable @next/next/no-location-assign-relative-destination */
export const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export async function userFetch<T>(path:string,init:RequestInit={}):Promise<T>{const token=localStorage.getItem("nusaguard_token");const response=await fetch(`${API}${path}`,{...init,headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`,...init.headers}});if(response.status===401){location.href=`/masuk?next=${encodeURIComponent(location.pathname+location.search)}`;throw new Error("Sesi berakhir.")}if(!response.ok){const body=await response.json().catch(()=>({}));throw new Error(body.detail??"Permintaan gagal.")}return response.status===204?({} as T):response.json()}
