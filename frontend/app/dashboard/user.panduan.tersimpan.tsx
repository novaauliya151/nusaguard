"use client";import {useEffect,useState} from "react";import {userFetch} from "./user-api";import styles from "./dashboard.module.css";
type Guide={id:string;title:string;category:string;description:string;updated_at:string};
export default function Guides(){const[rows,setRows]=useState<Guide[]>([]),[error,setError]=useState("");const load=()=>userFetch<Guide[]>("/api/user/saved-guides").then(setRows).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);return <section><div className={styles.pageHead}><div><p className={styles.kicker}>REFERENSI PRIBADI</p><h2>Panduan Tersimpan</h2></div><a href="/education">Cari panduan</a></div>{error?<div className={styles.state}>{error}</div>:rows.length===0?<div className={styles.state}>Belum ada panduan yang disimpan.</div>:<div className={styles.list}>{rows.map(row=><article key={row.id}><div><small>{row.category}</small><h3>{row.title}</h3><p>{row.description}</p></div><div className={styles.actions}><a href="/education">Baca</a><button onClick={async()=>{await userFetch(`/api/user/saved-guides/${row.id}`,{method:"DELETE"});void load()}}>Hapus</button></div></article>)}</div>}</section>}
