"use client";import {FormEvent,useState} from "react";import {userFetch} from "./user-api";import styles from "./dashboard.module.css";
export default function Profile({user,onUpdate}:{user:{name:string;email:string;last_login_at?:string;created_at?:string};onUpdate:(name:string)=>void}){const[name,setName]=useState(user.name),[current,setCurrent]=useState(""),[next,setNext]=useState(""),[message,setMessage]=useState("");async function profile(e:FormEvent){e.preventDefault();await userFetch("/api/user/profile",{method:"PATCH",body:JSON.stringify({name})});onUpdate(name);setMessage("Profil diperbarui.")}async function password(e:FormEvent){e.preventDefault();await userFetch("/api/user/password",{method:"POST",body:JSON.stringify({current_password:current,new_password:next})});setCurrent("");setNext("");setMessage("Kata sandi diperbarui.")}return <section><div className={styles.pageHead}><div><p className={styles.kicker}>AKUN SAYA</p><h2>Profil</h2></div></div><div className={styles.profileGrid}><form className={styles.settings} onSubmit={profile}><h3>Informasi akun</h3><label>Nama<input value={name} onChange={e=>setName(e.target.value)} minLength={2}/></label><label>Email<input disabled value={user.email}/></label><button>Simpan profil</button></form><form className={styles.settings} onSubmit={password}><h3>Ganti kata sandi</h3><label>Kata sandi saat ini<input required type="password" value={current} onChange={e=>setCurrent(e.target.value)}/></label><label>Kata sandi baru<input required minLength={8} type="password" value={next} onChange={e=>setNext(e.target.value)}/></label><button>Ganti kata sandi</button></form></div>{message&&<p>{message}</p>}</section>}
