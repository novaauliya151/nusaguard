"use client";import {useState} from "react";import type {AnalyzeResponse} from "../types";import {API,userFetch} from "./user-api";import styles from "./dashboard.module.css";
export default function UserAnalysis(){const[message,setMessage]=useState(""),[result,setResult]=useState<AnalyzeResponse|null>(null),[save,setSave]=useState(false),[saveText,setSaveText]=useState(true),[status,setStatus]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function analyze(){if(!message.trim())return;setLoading(true);setError("");setStatus("");try{const response=await fetch(`${API}/api/analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text:message,source:"user_dashboard"})});if(!response.ok)throw new Error();const body=await response.json();setResult(body);if(save)await persist(body)}catch{setError("Analisis gagal. Periksa koneksi backend.")}finally{setLoading(false)}}async function persist(body=result){if(!body)return;await userFetch("/api/user/histories",{method:"POST",body:JSON.stringify({text:message,save_text:saveText,category:body.category,risk_level:body.risk_level,risk_score:body.risk_score,confidence:body.confidence,summary:body.explanation,explanation:body.explanation,warning_signs:body.detected_patterns.map(x=>x.pattern),recommendations:body.recommendation,nseae_scores:body.nseae_scores,model_version:body.model_source})});setStatus("Hasil anonim berhasil disimpan ke Riwayat Saya.")}return <section><div className={styles.pageHead}><div><p className={styles.kicker}>ANALISIS PRIBADI</p><h2>Analisis Pesan Baru</h2></div></div><div className={styles.analyzer}><div><textarea maxLength={5000} value={message} onChange={e=>setMessage(e.target.value)} placeholder="Tempel pesan mencurigakan…"/><label><input type="checkbox" checked={save} onChange={e=>setSave(e.target.checked)}/> Simpan hasil ini ke Riwayat Saya</label>{save&&<label><input type="checkbox" checked={saveText} onChange={e=>setSaveText(e.target.checked)}/> Sertakan preview teks yang sudah dianonimkan</label>}<small>Teks asli tidak pernah disimpan. Penyimpanan dapat dimatikan melalui Privasi dan Keamanan.</small><div className={styles.meta}><span>{message.length}/5000</span><button disabled={loading||!message.trim()} onClick={()=>void analyze()}>{loading?"Menganalisis…":"Analisis sekarang"}</button></div>{error&&<p className={styles.error}>{error}</p>}{status&&<p>{status}</p>}</div><div className={`${styles.result} ${result?styles[result.risk_level.toLowerCase()]:""}`}>{!result?<div className={styles.empty}><span>⌁</span><h3>Hasil muncul di sini</h3><p>Analisis berjalan tanpa harus disimpan.</p></div>:<><span>RISIKO {result.risk_level}</span><strong>{Math.round(result.risk_score*100)}<small>/100</small></strong><h3>{result.category}</h3><p>{result.explanation}</p><div className={styles.riskScale} aria-label="Skala tingkat risiko"><span>LOW 0–39%</span><span>MEDIUM 40–69%</span><span>HIGH 70–100%</span></div><p className={styles.riskNote}>Confidence adalah keyakinan kategori; risk score merupakan gabungan klasifikasi dan indikator N-SEAE.</p><div className={styles.recommend}><b>Langkah aman</b><p>{result.recommendation}</p></div>{!save&&<button onClick={()=>void persist()}>Simpan hasil secara opsional</button>}</>}</div></div></section>}

