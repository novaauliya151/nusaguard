"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import Link from "next/link";import {useRouter,useSearchParams} from "next/navigation";import {useEffect,useState} from "react";import UserAnalysis from "./user.analisis";import UserDashboard from "./user.dashboard";import History from "./user.riwayat";import Reports from "./user.laporan";import Guides from "./user.panduan.tersimpan";import Privacy from "./user.privasi.keamanan";import Profile from "./user.profil";import {API} from "./user-api";import styles from "./dashboard.module.css";
type User={name:string;email:string;role:string;permissions:string[];last_login_at?:string;created_at?:string};type View="ringkasan"|"analisis"|"riwayat"|"laporan"|"panduan"|"profil"|"privasi";
const menus:[View,string,string][]=[["ringkasan","⌂","Ringkasan"],["analisis","⌁","Analisis Baru"],["riwayat","◫","Riwayat Analisis"],["laporan","◇","Laporan Saya"],["panduan","▥","Panduan Tersimpan"],["profil","○","Profil"],["privasi","⌾","Privasi dan Keamanan"]];
export default function UserShell(){const[user,setUser]=useState<User|null>(null),[menuOpen,setMenuOpen]=useState(false),[view,setView]=useState<View>("ringkasan");const router=useRouter(),search=useSearchParams();useEffect(()=>{const requested=search.get("fitur") as View|null;if(requested&&menus.some(x=>x[0]===requested))setView(requested);const token=localStorage.getItem("nusaguard_token");if(!token){router.replace(`/masuk?next=${encodeURIComponent('/dashboard')}`);return}fetch(`${API}/api/auth/me`,{headers:{Authorization:`Bearer ${token}`}}).then(async response=>{if(!response.ok)throw new Error();const account=await response.json();if(account.role!=="user"){router.replace("/admin");return}setUser(account)}).catch(()=>{localStorage.removeItem("nusaguard_token");router.replace("/masuk")})},[router,search]);async function logout(){const token=localStorage.getItem("nusaguard_token");if(token)await fetch(`${API}/api/auth/logout`,{method:"POST",headers:{Authorization:`Bearer ${token}`}}).catch(()=>null);localStorage.removeItem("nusaguard_token");router.push("/")}function navigate(next:View){setView(next);setMenuOpen(false);history.replaceState(null,"",`/dashboard?fitur=${next}`);window.scrollTo({top:0,behavior:"smooth"})}if(!user)return <main className={styles.loading}>Memuat pusat keamanan…</main>;const content={ringkasan:<UserDashboard openAnalyze={()=>navigate("analisis")}/>,analisis:<UserAnalysis/>,riwayat:<History/>,laporan:<Reports/>,panduan:<Guides/>,profil:<Profile user={user} onUpdate={name=>setUser({...user,name})}/>,privasi:<Privacy/>}[view];return <div className={`${styles.shell} ${menuOpen?styles.menuOpen:""}`}><button className={styles.backdrop} onClick={()=>setMenuOpen(false)} aria-label="Tutup menu"/><aside><div className={styles.sideHead}><Link className={styles.logo} href="/"><span>NG</span><b>NusaGuard</b></Link><button className={styles.closeMenu} onClick={()=>setMenuOpen(false)}>×</button></div><nav>{menus.map(([key,icon,label])=><button key={key} className={view===key?styles.active:""} onClick={()=>navigate(key)}>{icon} {label}</button>)}</nav><div className={styles.account}><i>{user.name.slice(0,2).toUpperCase()}</i><div><b>{user.name}</b><small>Pengguna</small></div><button onClick={()=>void logout()}>Keluar</button></div></aside><main><header><button className={styles.menuButton} onClick={()=>setMenuOpen(true)}>☰</button><div><p>DASHBOARD SAYA</p><h1>{view==="ringkasan"?`Halo, ${user.name.split(" ")[0]}`:menus.find(x=>x[0]===view)?.[2]}</h1><small>Dashboard / {menus.find(x=>x[0]===view)?.[2]}</small></div></header><section className={styles.content}>{content}</section><footer><span>NusaGuard · Akun opsional</span><span>{user.email}</span></footer></main></div>}
