"use client";
import Link from "next/link";import {FormEvent,useMemo,useState} from "react";import {useRouter} from "next/navigation";import styles from "../login/auth.module.css";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Register(){const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[terms,setTerms]=useState(false),[privacy,setPrivacy]=useState(false),[error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();const strength=useMemo(()=>[password.length>=8,/[A-Za-z]/.test(password),/\d/.test(password),/[^\w]/.test(password)].filter(Boolean).length,[password]);async function submit(e:FormEvent){e.preventDefault();if(password!==confirm){setError("Konfirmasi kata sandi tidak sama.");return}setLoading(true);setError("");try{const r=await fetch(`${API}/api/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,email,password,confirm_password:confirm,accept_terms:terms,accept_privacy:privacy})});const body=await r.json();if(!r.ok)throw new Error(body.detail);localStorage.setItem("nusaguard_token",body.access_token);router.push("/dashboard?onboarding=privasi")}catch(x){setError(x instanceof Error?x.message:"Registrasi gagal.")}finally{setLoading(false)}}return <main className={styles.shell}><section className={styles.formSide}><Link className={styles.brand} href="/"><span>NG</span><b>NusaGuard</b></Link><div className={styles.formWrap}><p className={styles.kicker}>AKUN OPSIONAL</p><h1>Buat akun pribadi.</h1><p>Analisis tetap bisa dilakukan tanpa akun. Akun berguna untuk menyimpan hasil yang kamu setujui.</p><form onSubmit={submit}><label>Nama lengkap<input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Kata sandi<input required type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/><small>Kekuatan: {['Sangat lemah','Lemah','Cukup','Kuat','Sangat kuat'][strength]}</small></label><label>Konfirmasi kata sandi<input required type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><label><input type="checkbox" required checked={terms} onChange={e=>setTerms(e.target.checked)}/> Saya menyetujui syarat penggunaan.</label><label><input type="checkbox" required checked={privacy} onChange={e=>setPrivacy(e.target.checked)}/> Saya menyetujui kebijakan privasi.</label>{error&&<div className={styles.error}>{error}</div>}<button disabled={loading}>{loading?"Membuat akun…":"Buat Akun"}</button></form><p className={styles.switch}>Sudah memiliki akun? <Link href="/masuk">Masuk</Link></p><Link className={styles.back} href="/">Tetap analisis tanpa login</Link></div></section></main>}
