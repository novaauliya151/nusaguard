"use client";
import {FormEvent,useCallback,useEffect,useState} from "react";import {adminFetch,AdminUser} from "./admin-api";import styles from "./admin.module.css";type Detail=AdminUser&{activities:{id:string;action:string;module:string|null;detail:string|null;created_at:string}[]};
export default function ProfilePage(){const[user,setUser]=useState<Detail|null>(null),[name,setName]=useState(""),[avatar,setAvatar]=useState(""),[current,setCurrent]=useState(""),[next,setNext]=useState(""),[confirm,setConfirm]=useState(""),[message,setMessage]=useState("");const load=useCallback(()=>adminFetch<Detail>("/api/admin/profile").then(value=>{setUser(value);setName(value.name);setAvatar(value.avatar??"")}).catch(e=>setMessage(e.message)),[]);useEffect(()=>{void load()},[load]);async function profile(e:FormEvent){e.preventDefault();try{await adminFetch("/api/admin/profile",{method:"PATCH",body:JSON.stringify({name,avatar:avatar||null})});setMessage("Profil berhasil diperbarui.");await load()}catch(x){setMessage(x instanceof Error?x.message:"Profil gagal diperbarui.")}}async function password(e:FormEvent){e.preventDefault();if(next!==confirm){setMessage("Konfirmasi password baru tidak sama.");return}try{await adminFetch("/api/admin/profile/password",{method:"POST",body:JSON.stringify({current_password:current,new_password:next})});setCurrent("");setNext("");setConfirm("");setMessage("Password berhasil diubah.")}catch(x){setMessage(x instanceof Error?x.message:"Gagal mengubah password.")}}return <><div className={styles.sectionHead}><div><span>ADMIN SECURITY</span><h2>Profil</h2></div></div>{message&&<div className={message.includes("berhasil")?styles.success:styles.alert}>{message}</div>}<div className={styles.grid}><form className={styles.userForm} onSubmit={profile}><h3>Informasi profil</h3><div className={styles.userIdentity}><i>{user?.name.slice(0,2).toUpperCase()}</i><div><b>{user?.email}</b><small>{user?.role.replaceAll("_"," ")}</small></div></div><label>Nama<input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/></label><label>URL avatar<input value={avatar} onChange={e=>setAvatar(e.target.value)}/></label><p><b>Bergabung:</b> {user?new Date(user.created_at).toLocaleString("id-ID"):"—"}</p><p><b>Login terakhir:</b> {user?.last_login_at?new Date(user.last_login_at).toLocaleString("id-ID"):"—"}</p><div className={styles.chips}>{user?.permissions.map(item=><small key={item}>{item}</small>)}</div><button>Simpan profil</button></form><form className={styles.userForm} onSubmit={password}><h3>Ganti password</h3><label>Password lama<input type="password" required minLength={8} value={current} onChange={e=>setCurrent(e.target.value)}/></label><label>Password baru<input type="password" required minLength={10} value={next} onChange={e=>setNext(e.target.value)}/></label><label>Konfirmasi password baru<input type="password" required minLength={10} value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button>Perbarui password</button></form><section className={styles.panel}><div className={styles.panelHead}><h3>Aktivitas saya</h3></div>{user?.activities.map(item=><div className={styles.systemRow} key={item.id}><i>●</i><div><b>{item.action}</b><small>{item.module??"sistem"} · {item.detail??"—"}</small></div><span>{new Date(item.created_at).toLocaleDateString("id-ID")}</span></div>)}</section></div></>}
