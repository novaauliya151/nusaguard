"use client";
import {FormEvent,useEffect,useState} from "react";import {adminFetch,API} from "./admin-api";import styles from "./admin.module.css";type User={name:string;email:string;role:string};export default function ProfilePage(){const[user,setUser]=useState<User|null>(null),[current,setCurrent]=useState(""),[next,setNext]=useState(""),[message,setMessage]=useState("");useEffect(()=>{const token=localStorage.getItem("nusaguard_token");const timer=setTimeout(()=>void fetch(`${API}/api/auth/me`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()).then(setUser),0);return()=>clearTimeout(timer)},[]);async function submit(e:FormEvent){e.preventDefault();try{await adminFetch("/api/admin/profile/password",{method:"POST",body:JSON.stringify({current_password:current,new_password:next})});setCurrent("");setNext("");setMessage("Password berhasil diubah.")}catch(x){setMessage(x instanceof Error?x.message:"Gagal mengubah password.")}}return <><div className={styles.sectionHead}><div><span>ADMIN SECURITY</span><h2>Profil dan keamanan</h2></div></div><div className={styles.userLayout}><section className={styles.panel}><h3>{user?.name??"Admin"}</h3><p>{user?.email}</p><span className={styles.roleBadge}>{user?.role}</span></section><form className={styles.userForm} onSubmit={submit}><h3>Ganti password</h3><label>Password lama<input type="password" required minLength={8} value={current} onChange={e=>setCurrent(e.target.value)}/></label><label>Password baru<input type="password" required minLength={8} value={next} onChange={e=>setNext(e.target.value)}/></label><button>Perbarui password</button>{message&&<p>{message}</p>}</form></div></>}
