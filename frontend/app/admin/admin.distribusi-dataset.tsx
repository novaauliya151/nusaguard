"use client";
import {useEffect,useState} from "react";import {adminFetch} from "./admin-api";import styles from "./admin.module.css";
type Distribution={categories:{category:string;count:number;target:number;percent:number}[];splits:Record<string,number>;sources:Record<string,number>;data_types:Record<string,number>;imbalanced:boolean;target_per_category:number};
export default function DistributionPage(){const[data,setData]=useState<Distribution|null>(null),[error,setError]=useState("");useEffect(()=>{void adminFetch<Distribution>("/api/admin/candidates/distribution").then(setData).catch(e=>setError(e.message))},[]);return <><div className={styles.sectionHead}><div><span>DATASET BALANCE</span><h2>Distribusi dataset</h2></div><small>Target {data?.target_per_category??500} data per kategori</small></div>{error&&<div className={styles.alert}>{error}</div>}{data?.imbalanced&&<div className={styles.warningBox}><b>Perhatian: distribusi kelas belum seimbang.</b><p>Prioritaskan kategori dengan progres terendah sebelum evaluasi model berikutnya.</p></div>}<div className={styles.cardGrid}>{data?.categories.map(item=><article className={styles.panel} key={item.category}><span className={styles.kicker}>{item.category}</span><h3 className={styles.bigNumber}>{item.count}</h3><div className={styles.bar}><b style={{width:`${item.percent}%`}}/></div><p>{item.percent}% dari target {item.target}</p></article>)}</div><div className={styles.grid}><Breakdown title="Pembagian train/validation/test" data={data?.splits}/><Breakdown title="Jenis data" data={data?.data_types}/><Breakdown title="Sumber data" data={data?.sources}/></div></>}
function Breakdown({title,data}:{title:string;data?:Record<string,number>}){return <section className={styles.panel}><div className={styles.panelHead}><h3>{title}</h3></div>{Object.entries(data??{}).map(([key,value])=><div className={styles.systemRow} key={key}><i>●</i><div><b>{key}</b><small>Data anonim tervalidasi</small></div><strong>{value}</strong></div>)}{!Object.keys(data??{}).length&&<div className={styles.empty}>Belum ada data.</div>}</section>}
