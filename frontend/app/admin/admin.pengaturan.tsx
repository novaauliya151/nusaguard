"use client";
import {FormEvent,useCallback,useEffect,useState} from "react";import {adminFetch} from "./admin-api";import styles from "./admin.module.css";
type Settings=Record<string,Record<string,string>>;
export default function SettingsPage(){const[values,setValues]=useState<Settings>({}),[message,setMessage]=useState(""),[saving,setSaving]=useState(false);const load=useCallback(()=>adminFetch<Settings>("/api/admin/settings").then(setValues).catch(e=>setMessage(e.message)),[]);useEffect(()=>{void load()},[load]);function set(group:string,key:string,value:string){setValues(current=>({...current,[group]:{...current[group],[key]:value}}))}async function save(e:FormEvent){e.preventDefault();setSaving(true);try{setValues(await adminFetch<Settings>("/api/admin/settings",{method:"PUT",body:JSON.stringify({values})}));setMessage("Pengaturan berhasil disimpan dan dicatat dalam log.")}catch(x){setMessage(x instanceof Error?x.message:"Pengaturan gagal disimpan.")}finally{setSaving(false)}}return <><div className={styles.sectionHead}><div><span>SYSTEM CONFIGURATION</span><h2>Pengaturan</h2></div></div>{message&&<div className={message.includes("berhasil")?styles.success:styles.alert}>{message}</div>}<form className={styles.settingsGrid} onSubmit={save}>{Object.entries(values).map(([group,items])=><fieldset className={styles.panel} key={group}><legend>{group.replaceAll("_"," ")}</legend>{Object.entries(items).map(([key,value])=><label key={key}>{key.replaceAll("_"," ")} {value==="true"||value==="false"?<select value={value} onChange={e=>set(group,key,e.target.value)}><option value="true">Aktif</option><option value="false">Nonaktif</option></select>:<input value={value} onChange={e=>set(group,key,e.target.value)}/>}</label>)}</fieldset>)}<button className={styles.createButton} disabled={saving}>{saving?"Menyimpan…":"Simpan semua pengaturan"}</button></form></>}
