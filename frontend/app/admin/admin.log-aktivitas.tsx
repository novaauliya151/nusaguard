"use client";
import {useEffect,useState} from "react";import {adminFetch} from "./admin-api";import styles from "./admin.module.css";type Log={id:string;admin_email:string;action:string;object_type:string;object_id:string|null;detail:string|null;created_at:string};export default function ActivityPage(){const[logs,setLogs]=useState<Log[]>([]);useEffect(()=>{const timer=setTimeout(()=>void adminFetch<Log[]>("/api/admin/activities").then(setLogs),0);return()=>clearTimeout(timer)},[]);return <><div className={styles.sectionHead}><div><span>AUDIT TRAIL · READ ONLY</span><h2>Riwayat aktivitas admin</h2></div></div><section className={`${styles.panel} ${styles.userTable}`}><div className={styles.tableWrap}><table><thead><tr><th>Waktu</th><th>Admin</th><th>Aktivitas</th><th>Objek</th><th>Keterangan</th></tr></thead><tbody>{logs.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString("id-ID")}</td><td>{x.admin_email}</td><td>{x.action}</td><td>{x.object_type}</td><td>{x.detail??"—"}</td></tr>)}</tbody></table></div></section></>}
