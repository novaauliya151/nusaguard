"use client";
import {useEffect,useMemo,useState} from "react";import {adminFetch} from "./admin-api";import styles from "./admin.module.css";type Log={id:string;admin_email:string;action:string;object_type:string;object_id:string|null;detail:string|null;module:string|null;ip_address:string|null;created_at:string};export default function ActivityPage(){const[logs,setLogs]=useState<Log[]>([]),[query,setQuery]=useState(""),[module,setModule]=useState("all"),[error,setError]=useState(""),[page,setPage]=useState(1);useEffect(()=>{void adminFetch<Log[]>("/api/admin/activities").then(setLogs).catch(e=>setError(e.message))},[]);const filtered=useMemo(()=>logs.filter(item=>(module==="all"||(item.module??item.object_type)===module)&&(!query||`${item.admin_email} ${item.action} ${item.detail}`.toLowerCase().includes(query.toLowerCase()))),[logs,module,query]),rows=filtered.slice((page-1)*20,page*20),modules=[...new Set(logs.map(item=>item.module??item.object_type))];return <><div className={styles.sectionHead}><div><span>AUDIT TRAIL · READ ONLY</span><h2>Log aktivitas</h2></div></div><div className={styles.userToolbar}><input value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}} placeholder="Cari admin, aktivitas, atau keterangan…"/><select value={module} onChange={e=>{setModule(e.target.value);setPage(1)}}><option value="all">Semua modul</option>{modules.map(item=><option key={item}>{item}</option>)}</select><span>{filtered.length} aktivitas</span></div>{error&&<div className={styles.alert}>{error}</div>}<section className={`${styles.panel} ${styles.userTable}`}><div className={styles.tableWrap}><table><thead><tr><th>Waktu</th><th>Admin</th><th>Aktivitas</th><th>Modul/objek</th><th>Keterangan</th><th>IP</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString("id-ID")}</td><td>{x.admin_email}</td><td>{x.action}</td><td>{x.module??x.object_type}<small>{x.object_id}</small></td><td>{x.detail??"—"}</td><td>{x.ip_address??"Tidak dicatat"}</td></tr>)}</tbody></table></div>{!rows.length&&<div className={styles.empty}>Belum ada log pada filter ini.</div>}<div className={styles.pagination}><button disabled={page<=1} onClick={()=>setPage(p=>p-1)}>Sebelumnya</button><span>Halaman {page}</span><button disabled={page*20>=filtered.length} onClick={()=>setPage(p=>p+1)}>Berikutnya</button></div></section></>}
