export function parseCsv(input:string):string[][]{const rows:string[][]=[],row:string[]=[],push=()=>{row.push(cell);cell="";};let cell="",quoted=false;for(let i=0;i<input.length;i++){const char=input[i];if(char==='"'&&quoted&&input[i+1]==='"'){cell+='"';i++;continue}if(char==='"'){quoted=!quoted;continue}if(char===","&&!quoted){push();continue}if((char==="\n"||char==="\r")&&!quoted){if(char==="\r"&&input[i+1]==="\n")i++;push();if(row.some(value=>value.trim()))rows.push([...row]);row.length=0;continue}cell+=char}push();if(row.some(value=>value.trim()))rows.push(row);return rows}
